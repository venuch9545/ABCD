# Task: Convert to multi-page site

## Pages
1. `/` — Home (Hero only + brief intro)
2. `/about` — About (full story, stats)
3. `/products` — Products (gallery + modals)
4. `/certifications` — Certifications (cards + cert view)
5. `/contact` — Contact (all contact cards, socials, CTA)

## Files to create/update
- [x] `src/web/components/Navbar.tsx` — change anchor links to route links using `<Link>`
- [x] `src/web/components/Layout.tsx` — shared layout (Navbar + Footer + floating WA)
- [x] `src/web/pages/index.tsx` — Home page (Hero + brief CTA)
- [x] `src/web/pages/about.tsx` — About page
- [x] `src/web/pages/products.tsx` — Products page
- [x] `src/web/pages/certifications.tsx` — Certifications page (includes cert detail view)
- [x] `src/web/pages/contact.tsx` — Contact page
- [x] `src/web/app.tsx` — update routes
- [ ] Remove old certificate.tsx page
